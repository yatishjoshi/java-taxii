language: java

jdk:
  - oraclejdk8

# the below keeps 'gradle assemble' from being called which tries to sign the jars.
install: 
  gradle clean -Pgroup=com.github.yatishjoshi -Pversion=v1.1.0.2-latest -xtest install


